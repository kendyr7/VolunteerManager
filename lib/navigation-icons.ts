// Shared by navigation and notifications so each destination keeps its identity.
export const NAVIGATION_ICONS = {
  dashboard: 'space_dashboard',
  requests: 'published_with_changes',
  settings: 'settings',
} as const;
